experiment:
    id: smoke_student
data:
    data_dir: ''  # Set TRAIN_DATABASE instead of committing a host path.
    resolution: 0.05
    dataloader: 'KITTI'
    split: 'train'
    train: ['00']
    test: ['00']
    num_points: 180000
    max_range: 50.
    dataset_norm: False
    std_axis_norm: False
train:
    n_gpus: 1
    num_workers: 0
    max_epoch: 1
    limit_train_batches: 1
    limit_test_batches: 1
    lr: 0.0001
    batch_size: 1
    teacher_checkpoint: ''  # Fill with the new smoke_teacher checkpoint.
model:
    out_dim: 96
