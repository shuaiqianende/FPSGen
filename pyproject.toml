[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fpsgen"
version = "0.1.0"
description = "FPSGen: conditional BEV and point-cloud generation"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["fpsgen*"]
